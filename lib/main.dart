import 'package:flutter/material.dart';
import 'core/ecommerce_app.dart';

void main() async {
  // Basic Flutter initialization
  WidgetsFlutterBinding.ensureInitialized();

  // Start the app immediately
  runApp(const EcommerceApp());
}